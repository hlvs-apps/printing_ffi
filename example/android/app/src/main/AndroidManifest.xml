<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: the USB host feature, the FGS <service>, and the FOREGROUND_SERVICE /
         FOREGROUND_SERVICE_CONNECTED_DEVICE / POST_NOTIFICATIONS permissions are all
         provided by the printing_ffi plugin's manifest and merge in automatically.
         The only Android-side bits a consumer app adds are on the launcher <activity>
         below: singleTop launchMode + the USB_DEVICE_ATTACHED intent-filter +
         <meta-data> pointing at the plugin's @xml/printing_ffi_usb_device_filter. -->
    <application
        android:label="printing_ffi_example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Offer to open the app when a matching DNP/Citizen USB printer is
                 plugged in, and let the user tick "use by default" (persists the
                 USB permission for that device). The device_filter lists the exact
                 DNP/Citizen VID/PIDs the Gutenprint dnpds40 backend supports. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/printing_ffi_usb_device_filter" />
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
